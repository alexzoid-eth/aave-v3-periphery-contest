diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..13d5382 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -133,6 +133,7 @@ abstract contract RewardsDistributor is IRewardsDistributor {
     address user,
     address reward
   ) external view override returns (uint256) {
+    assert(false);
     return _getUserReward(user, reward, _getUserAssetBalances(assets, user));
   }
 
