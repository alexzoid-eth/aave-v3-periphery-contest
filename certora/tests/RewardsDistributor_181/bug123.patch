diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..05fb269 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -362,7 +362,8 @@ abstract contract RewardsDistributor is IRewardsDistributor {
       assetUnit = 10**_assets[asset].decimals;
     }
 
-    if (numAvailableRewards == 0) {
+    /// IfStatementMutation(`numAvailableRewards == 0` |==> `false`) of: `if (numAvailableRewards == 0) {`
+    if (false) {
       return;
     }
     unchecked {
