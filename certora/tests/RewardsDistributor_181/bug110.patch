diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..b716875 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -337,7 +337,8 @@ abstract contract RewardsDistributor is IRewardsDistributor {
       if (userBalance != 0) {
         rewardsAccrued = _getRewards(userBalance, newAssetIndex, userIndex, assetUnit);
 
-        rewardData.usersData[user].accrued += rewardsAccrued.toUint128();
+        /// DeleteExpressionMutation(`rewardData.usersData[user].accrued += rewardsAccrued.toUint128()` |==> `assert(true)`) of: `rewardData.usersData[user].accrued += rewardsAccrued.toUint128();`
+        assert(true);
       }
     }
     return (rewardsAccrued, dataUpdated);
