diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..787daf9 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -480,7 +480,8 @@ abstract contract RewardsDistributor is IRewardsDistributor {
     uint256 userIndex,
     uint256 assetUnit
   ) internal pure returns (uint256) {
-    uint256 result = userBalance * (reserveIndex - userIndex);
+    /// SwapArgumentsOperatorMutation(`reserveIndex - userIndex` |==> `userIndex - reserveIndex`) of: `uint256 result = userBalance * (reserveIndex - userIndex);`
+    uint256 result = userBalance * (userIndex - reserveIndex);
     assembly {
       result := div(result, assetUnit)
     }
