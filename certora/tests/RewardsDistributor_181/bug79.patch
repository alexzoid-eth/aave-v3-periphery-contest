diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..1887b72 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -230,7 +230,8 @@ abstract contract RewardsDistributor is IRewardsDistributor {
    * @param rewardsInput The array of each asset configuration
    **/
   function _configureAssets(RewardsDataTypes.RewardsConfigInput[] memory rewardsInput) internal {
-    for (uint256 i = 0; i < rewardsInput.length; i++) {
+    /// DeleteExpressionMutation(`i++` |==> `assert(true)`) of: `for (uint256 i = 0; i < rewardsInput.length; i++) {`
+    for (uint256 i = 0; i < rewardsInput.length; assert(true)) {
       if (_assets[rewardsInput[i].asset].decimals == 0) {
         //never initialized before, adding to the list of assets
         _assetsList.push(rewardsInput[i].asset);
