diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..373a244 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -359,7 +359,8 @@ abstract contract RewardsDistributor is IRewardsDistributor {
     uint256 assetUnit;
     uint256 numAvailableRewards = _assets[asset].availableRewardsCount;
     unchecked {
-      assetUnit = 10**_assets[asset].decimals;
+      /// DeleteExpressionMutation(`assetUnit = 10**_assets[asset].decimals` |==> `assert(true)`) of: `assetUnit = 10**_assets[asset].decimals;`
+      assert(true);
     }
 
     if (numAvailableRewards == 0) {
