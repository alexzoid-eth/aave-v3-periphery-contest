diff --git a/contracts/rewards/RewardsDistributor.sol b/contracts/rewards/RewardsDistributor.sol
index 04d057a..2c5f1f3 100644
--- a/contracts/rewards/RewardsDistributor.sol
+++ b/contracts/rewards/RewardsDistributor.sol
@@ -255,7 +255,8 @@ abstract contract RewardsDistributor is IRewardsDistributor {
       // Add reward address to global rewards list if still not enabled
       if (_isRewardEnabled[rewardsInput[i].reward] == false) {
         _isRewardEnabled[rewardsInput[i].reward] = true;
-        _rewardsList.push(rewardsInput[i].reward);
+        /// DeleteExpressionMutation(`_rewardsList.push(rewardsInput[i].reward)` |==> `assert(true)`) of: `_rewardsList.push(rewardsInput[i].reward);`
+        assert(true);
       }
 
       // Due emissions is still zero, updates only latestUpdateTimestamp
