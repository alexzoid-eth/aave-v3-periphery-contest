diff --git a/contracts/rewards/RewardsController.sol b/contracts/rewards/RewardsController.sol
index cfa4d82..4d57aaa 100644
--- a/contracts/rewards/RewardsController.sol
+++ b/contracts/rewards/RewardsController.sol
@@ -230,7 +230,8 @@ contract RewardsController is RewardsDistributor, VersionedInitializable, IRewar
     address to,
     address reward
   ) internal returns (uint256) {
-    if (amount == 0) {
+    /// IfStatementMutation(`amount == 0` |==> `true`) of: `if (amount == 0) {`
+    if (true) {
       return 0;
     }
     uint256 totalRewards;
