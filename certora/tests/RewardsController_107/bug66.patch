diff --git a/contracts/rewards/RewardsController.sol b/contracts/rewards/RewardsController.sol
index cfa4d82..c135898 100644
--- a/contracts/rewards/RewardsController.sol
+++ b/contracts/rewards/RewardsController.sol
@@ -250,7 +250,8 @@ contract RewardsController is RewardsDistributor, VersionedInitializable, IRewar
       }
     }
 
-    if (totalRewards == 0) {
+    /// IfStatementMutation(`totalRewards == 0` |==> `true`) of: `if (totalRewards == 0) {`
+    if (true) {
       return 0;
     }
 
