diff --git a/contracts/rewards/RewardsController.sol b/contracts/rewards/RewardsController.sol
index cfa4d82..b97b0af 100644
--- a/contracts/rewards/RewardsController.sol
+++ b/contracts/rewards/RewardsController.sol
@@ -284,7 +284,8 @@ contract RewardsController is RewardsDistributor, VersionedInitializable, IRewar
 
     for (uint256 i = 0; i < assets.length; i++) {
       address asset = assets[i];
-      for (uint256 j = 0; j < rewardsListLength; j++) {
+      /// SwapArgumentsOperatorMutation(`j < rewardsListLength` |==> `rewardsListLength < j`) of: `for (uint256 j = 0; j < rewardsListLength; j++) {`
+      for (uint256 j = 0; rewardsListLength < j; j++) {
         if (rewardsList[j] == address(0)) {
           rewardsList[j] = _rewardsList[j];
         }
